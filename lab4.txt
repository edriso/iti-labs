CREATE DATABASE php_lab5;

CREATE TABLE users (
	user_id int AUTO_INCREMENT,
	name varchar(30) NOT NULL,
	password varchar(40) NOT NULL,
	PRIMARY KEY(user_id)
);

INSERT INTO users (name, password)
VALUES ("Otto", asd123");

SELECT * FROM users
WHERE name = 'Otto' AND password = 'asd123';